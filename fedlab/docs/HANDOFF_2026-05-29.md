# FedLab Handoff — 2026-05-29

Pivot from "decentralized federated training network" framing to a **verified
model registry + validation layer** MVP for the Bags Hackathon. This brief is the
single source of truth for picking the work back up tomorrow.

## Updated idea outline

FedLab starts as a **Bags-powered verified model registry**. It begins with a
curated array of models, each verified by a benchmark *validation packet* — not a
complex decentralized training network.

1. A registry tracks models, each with a domain, benchmark, score, and status.
2. Each domain has a validation packet: dataset summary, metric, evaluation
   script, current best score, leakage checks.
3. A passing validation produces a validation hash → a public proof of validation
   that can be anchored on Solana.
4. Bags supplies the project/token traction layer on top of the registry.

Initial tracks: **SWE-bench++** (software engineering), **TabPFN** (tabular),
**Reservoir Finance** (financial forecasting).

## What changed today

- Added a file-backed verified model registry (`registry/`).
- Added three validation packets (`validation_packets/`), two with runnable,
  dependency-light evaluators.
- Extended the existing FastAPI aggregator with registry / packet / integration
  endpoints (existing federated endpoints kept).
- Added Bags and Solana integration scaffolds (mock/not-configured until env set).
- Rebuilt the frontend around the registry story: new home headline, Registry
  table page, Validation Packets page, Bags+Web3 integration hooks card.
- Rewrote both READMEs to make the registry MVP primary; federated training is now
  labeled "future direction".

## Files created / modified

**Created**
- `registry/__init__.py`, `registry/models.json`, `registry/registry.py`
- `validation_packets/__init__.py`, `validation_packets/packets.py`
- `validation_packets/swe_bench_plus_plus/{__init__.py,packet.json,README.md}`
- `validation_packets/tabpfn/{__init__.py,packet.json,evaluate.py}`
- `validation_packets/reservoir_finance/{__init__.py,packet.json,evaluate.py}`
- `integrations/__init__.py`, `integrations/bags.py`, `integrations/solana.py`
- `frontend/lib/registryData.js`, `frontend/pages/registry.js`,
  `frontend/pages/packets.js`
- `docs/HANDOFF_2026-05-29.md` (this file)

**Modified**
- `aggregator/server.py` (new endpoints + imports)
- `frontend/pages/_app.js` (nav), `frontend/pages/index.js` (home),
  `frontend/styles/globals.css` (badge / packet-grid / code-block styles)
- `README.md` (root) and `fedlab/README.md`

## Backend / API plan and endpoints

FastAPI app at `aggregator/server.py`, file-backed state. New endpoints:

- `GET  /registry/models` — list models
- `GET  /registry/models/{model_id}` — one model
- `POST /registry/submit` — queue a model; returns a `candidate` row (no
  validation run yet)
- `GET  /validation-packets` — list packet metadata
- `GET  /validation-packets/{packet_id}` — one packet
- `GET  /integrations/status` — Bags + Solana config status
- `GET  /integrations/bags/project` — project stats (mock if unconfigured)
- `GET  /integrations/bags/token` — token stats (mock if unconfigured)

Existing: `/health`, `/model/latest`, `/leaderboard`, `/contribute`.

## Bags API integration plan + env vars

`integrations/bags.py`: `get_project_stats()`, `get_token_stats()`,
`get_integration_status()`. Returns `configured: false` / `mode: "mock"` when env
is missing. Endpoint paths are configurable placeholders; no private API is called.

Env: `BAGS_API_BASE_URL`, `BAGS_API_KEY`, `FEDLAB_BAGS_PROJECT_ID`,
`FZIQ_TOKEN_ADDRESS`.

## Web3 / Solana integration plan + env vars

`integrations/solana.py`: `is_valid_wallet()`, `build_validation_proof(model)`
(sha256 over validation event → anchorable hash), `check_onchain_proof(hash)`,
`get_integration_status()`. **No contract, no mint, no transfer.**

Env: `SOLANA_RPC_URL`, `FZIQ_TOKEN_ADDRESS`.

## How to run / check it

```bash
# Python compile + module smoke test
cd fedlab
python -m compileall fedlab        # from repo root: python -m compileall fedlab

# API
uvicorn aggregator.server:app --reload
curl localhost:8000/registry/models
curl localhost:8000/validation-packets
curl localhost:8000/integrations/status

# Packet evaluators (dependency-light)
python -m validation_packets.tabpfn.evaluate --predictions preds.json
python -m validation_packets.reservoir_finance.evaluate --predictions preds.json

# Frontend
cd frontend && npm install && npm run build && npm run dev
```

## Demo vs. truly verified

- **Demo/candidate (not measured):** the three seed scores (`18.4% pass`,
  `0.91 ROC-AUC`, `0.037 RMSE`); SWE-bench++ harness; live Bags stats; on-chain
  anchoring.
- **Truly working:** registry CRUD-lite, packet metadata, the tabpfn and
  reservoir evaluators (produce real metrics from a predictions file), all API
  endpoints, mock integration responses, frontend build.

## What to fine-tune tomorrow for Bags submission

1. Replace one seed score with a *real* small run (tabpfn or reservoir evaluator
   on a tiny held-out file) so at least one row is genuinely measured.
2. Wire `/registry/submit` to kick a packet eval and flip status candidate →
   verified, writing the `validation_hash`.
3. Set Bags env vars and confirm `/integrations/bags/*` returns live data; screen-
   shot it for the submission.
4. Polish the Registry/Packets pages (copy, spacing) and record a 60–90s demo.
5. Tighten the pitch around "auditable model verification with an on-chain proof
   path" — concrete, honest, and Bags-native.

## Suggested next 5 tasks

1. Add a real predictions fixture + a `make verify` that runs both evaluators and
   writes results back into `models.json`.
2. Implement validation-hash write-back on submit (`solana.build_validation_proof`
   already produces the hash).
3. Confirm real Bags endpoint paths from docs; replace placeholder path templates.
4. Add a small `/integrations/solana/proof/{model_id}` endpoint returning the
   proof payload for a model.
5. Frontend: fetch live data from the API when `NEXT_PUBLIC_AGGREGATOR_URL` is
   reachable, falling back to the static mirror.

## Suggested application language for Bags

> FedLab is a verified model registry: an auditable directory of AI models that
> have each passed a standardized, leakage-checked validation packet (SWE-bench++
> for code, TabPFN for tabular, a reservoir packet for finance). Every verified
> result produces a validation hash with an on-chain proof path on Solana, and
> FedLab is built Bags-native — project and token traction flow directly through
> the Bags API. We're starting with real, honest benchmarks and a clear proof
> path, not promises.

## Caveats / limitations

- Seed scores are illustrative targets, labeled demo/candidate. Do not present
  them as measured.
- SWE-bench++ is a packet *definition*; the execution harness is not built.
- Integrations return mock/not-configured data until credentials are supplied.
- No Solana contract or token mechanics are implemented (intentional).
- State is file-backed and single-process — fine for the hackathon, not for scale.
